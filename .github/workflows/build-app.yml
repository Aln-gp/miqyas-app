name: Build CarPlay Final App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Compile and Bundle IPA
      run: |
        SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
        
        # 1. بناء ملفات السويفت مجتمعة
        swiftc -target arm64-apple-ios15.0 \
          -sdk "$SDK_PATH" \
          -O \
          *.swift \
          -framework SwiftUI \
          -framework CarPlay \
          -framework WebKit \
          -framework ReplayKit \
          -o CarPlayHub
          
        # 2. تجهيز مجلد الحزمة
        mkdir -p Payload/CarPlayHub.app
        mv CarPlayHub Payload/CarPlayHub.app/CarPlayHub
        
        # 3. حقن ملف صلاحيات السيارة الحقيقي والتخطي الكامل للقيود
        cat <<EOF > Payload/CarPlayHub.app/archived-with-entitlements.xcent
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>com.apple.developer.carplay-maps</key>
            <true/>
            <key>com.apple.developer.playable-content</key>
            <true/>
        </dict>
        </plist>
        EOF
        
        # 4. توليد ملف Info.plist لدعم تشغيل المتصفحات داخل شاشة الكاربلاي
        cat <<EOF > Payload/CarPlayHub.app/Info.plist
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleIdentifier</key>
            <string>com.yazan.carplayfinal</string>
            <key>CFBundleName</key>
            <string>CarPlayHub</string>
            <key>CFBundleExecutable</key>
            <string>CarPlayHub</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleShortVersionString</key>
            <string>1.0</string>
            <key>CFBundleVersion</key>
            <string>1</string>
            <key>UIApplicationSceneManifest</key>
            <dict>
                <key>UIApplicationSupportsMultipleScenes</key>
                <true/>
                <key>UISceneConfigurations</key>
                <dict>
                    <key>UIWindowSceneSessionRoleApplication</key>
                    <array>
                        <dict>
                            <key>UISceneConfigurationName</key>
                            <string>Default Configuration</string>
                            <key>UISceneDelegateClassName</key>
                            <string>CarPlayHub.SceneDelegate</string>
                        </dict>
                    </array>
                    <key>CPTemplateApplicationSceneSessionRoleApplication</key>
                    <array>
                        <dict>
                            <key>UISceneConfigurationName</key>
                            <string>CarPlay</string>
                            <key>UISceneDelegateClassName</key>
                            <string>CarPlayHub.CarPlaySceneDelegate</string>
                        </dict>
                    </array>
                </dict>
            </dict>
        </dict>
        </plist>
        EOF

        # 5. ضغط الملف بصيغة ipa
        zip -r CarPlayFinal.ipa Payload
        
    - name: Upload Finished IPA
      uses: actions/upload-artifact@v4
      with:
        name: CarPlay-Final-TrollStore
        path: CarPlayFinal.ipa
        
