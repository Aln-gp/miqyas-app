name: Build Najd OS

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Build Najd OS
      run: |
        dotnet restore NajdOS.sln
        dotnet build NajdOS.sln --configuration Release

    - name: Find and Upload ISO Artifact
      uses: actions/upload-artifact@v4
      with:
        name: NajdOS-Complete-ISO
        # هنا نخليه يبحث تلقائياً عن ملف الـ ISO في كل مجلدات النظام ويرفعه مباشرة بدون تحديد مجلد مخرج معين
        path: |
          **/bin/Release/net8.0/*.iso
          **/*.iso
        if-no-files-found: error
        
